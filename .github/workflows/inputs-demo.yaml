name: inputs-demo
on: 
    workflow_dispatch:
        inputs:
            env:
                description: Choose the environment to display
                required: true
                default: "dev"
jobs:
    inputs-demo:
        runs-on: ubuntu-latest
        steps:
            - name: Deploy the app
              run: echo "Deploying to ${{ inputs.env}} environment"
 