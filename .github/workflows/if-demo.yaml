name: if demo
on:
    push:
        branches:
            - main
            - develop
jobs:
    if-demo:
        runs-on: ubuntu-latest
        steps:
            - name: Run this  step if 
              if: github.ref_name == 'main'
              run: echo "Running for main branch"
            
            - name: Run this step
              run: echo "Running this step"
    

