
name: Github Action Demo
on: push
jobs:
  action-demo:
   runs-on: ubuntu-latest
   steps:
    - name: Checkout code
      uses: actions/checkout@v3
    - name: Print index.html file
      run: cat index.html
  