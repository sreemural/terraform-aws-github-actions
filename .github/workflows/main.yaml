name: hello-world
on: 
    push:
        branches:
            - main
            - feature
            - develop
    pull_request:
        branches:
            - main
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Hello world action step
              run: echo "Hello world!"
